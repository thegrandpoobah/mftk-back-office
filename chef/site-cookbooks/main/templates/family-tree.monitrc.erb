check process mftk-back-office with pidfile "/var/run/mftk-back-office.pid"
  start program = "/bin/bash -c 'source /srv/www/shared/app.env ; /usr/local/bin/mftk-back-office >> /var/log/mftk-back-office.log 2>&1'"
  stop program = "/usr/bin/pkill -F /var/run/mftk-back-office.pid"
  if failed port 80 protocol HTTP
  	request /health.html
  	with timeout 10 seconds
  	then restart
